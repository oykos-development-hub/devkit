export interface FontSize {
    xs: string;
    sm: string;
    md: string;
    lg: string;
    xl: string;
    xxl: string;
}
export interface LineHeight extends FontSize {
}
export interface FontFamily {
    one: string;
}
