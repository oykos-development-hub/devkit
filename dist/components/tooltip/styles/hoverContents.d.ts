import { TooltipPositions } from "../types";
export declare const HoverContents: import("styled-components").StyledComponent<"div", import("styled-components").DefaultTheme, {
    position: TooltipPositions | `${TooltipPositions}`;
    tooltipWidth: number;
}, never>;
