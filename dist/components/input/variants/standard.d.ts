import React from "react";
import { InputProps } from "../types";
export declare const Standard: ({ variant, type, onChange, onBlur, onFocus, placeholder, name, id, value, pattern, inputMode, maxLength, inputRef, ...props }: InputProps) => React.ReactElement;
