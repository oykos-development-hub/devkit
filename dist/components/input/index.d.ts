import React from "react";
import { InputProps } from "./types";
export declare const Input: ({ name, value, theme, style, disabled, label, textarea, leftContent, rightContent, error, hint, placeholder, onChange, onBlur, onFocus, id, ...props }: InputProps) => React.ReactElement;
