/// <reference types="react" />
import { ButtonProps } from "./types";
export declare const Button: ({ onClick, content, customContent, disabled, variant, size, style, theme, className, }: ButtonProps) => JSX.Element;
