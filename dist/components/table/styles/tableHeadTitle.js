import styled from "styled-components";
import { SSSTypography } from "../../typography";
import { Theme } from "../../../shared/theme";
const TableHeadTitle = styled(SSSTypography) `
  font-weight: 600;
  text-align: start;
  color: ${Theme.palette.gray600};
`;
export default TableHeadTitle;
//# sourceMappingURL=tableHeadTitle.js.map