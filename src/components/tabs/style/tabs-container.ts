import styled, { css } from "styled-components";

export const TabsContainer = styled.div(() => {
  return css`
    display: flex;
    list-style: none;
    align-items: center;
    gap: 0.1875em;
  `;
});
