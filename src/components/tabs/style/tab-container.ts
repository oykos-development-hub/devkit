import styled, { css } from "styled-components";

export const TabContainer = styled.div(() => {
  return css`
    display: flex;
    list-style: none;
    align-items: center;
    gap: 2.75em;
  `;
});
